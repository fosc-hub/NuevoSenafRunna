/**
 * @deprecated Re-export shim. The canonical location for FileUploadSection is
 *   `@/components/shared/FileUploadSection`. New code must import from there.
 *   This file exists only so that pre-existing imports keep working until they
 *   are migrated.
 */
export {
  FileUploadSection,
  type FileItem,
  type FileUploadSectionProps,
  type UploadCallback,
} from "@/components/shared/FileUploadSection"
